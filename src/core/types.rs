#[derive(Debug, Clone, Copy, PartialEq)]
pub enum AstoStructure {

    Input,
    Description,
    Version,
    Type,
    Info,

    Param,
    ParamBox,
    // OutputLine,
    // OutputBox,
    
    Tab,
    NewLine
    
}
